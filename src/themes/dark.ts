const darkTheme = {
  primaryColor: '#fff',
  secondaryColor: '#eee',
  bgColor: '#242938',
  bgButton: '#fff',
  borderColor: '#c1c1c1'
}

export default darkTheme

export type Theme = {
  primaryColor: string
  secondaryColor: string
  bgColor: string
  bgButton: string
  borderColor: string
}
